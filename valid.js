var csv_parse = require('babyparse');


// checks if the file inputed is a csv file, if true, parse it into a JSON file
function parsefile(){
	var file = document.getElementById("exampleInputFile"); 
	console.log(file);
};